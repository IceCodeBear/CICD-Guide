import React from "react";
import { Container, Header } from "semantic-ui-react";

const AWS = () => (
    <Container text>
        <Header as="h1">Setup CICD on AWS with Github</Header>
    </Container>
);

export default AWS;